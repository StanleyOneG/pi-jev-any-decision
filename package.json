{
  "name": "pi-jev-any-decision",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Project-local, opt-in delegation assessment for Pi.",
  "scripts": {
    "test": "PI_SUBAGENT_CHILD=0 node --experimental-strip-types --test test/**/*.test.ts",
    "typecheck": "tsc --noEmit",
    "compare": "node scripts/compare.mjs"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.86.1",
    "@earendil-works/pi-coding-agent": "^0.86.1",
    "@typesafe-ai/sdk": "^0.6.0",
    "pi-subagents": "^0.70.1",
    "typebox": "^1.3.34"
  },
  "devDependencies": {
    "@types/node": "^22.19.0",
    "typescript": "^5.9.3"
  }
}
